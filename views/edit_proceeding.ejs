<!DOCTYPE html>
<html>
<head>
  <title>Edit Proceeding</title>
</head>
<body>
  <h1>Edit Proceeding</h1>
  <% if (error) { %>
    <p style="color: red;"><%= error %></p>
  <% } %>
  <form method="POST" action="/edit_proceeding/<%= proceeding.id %>">
    <label>Proceeding Date (YYYY-MM-DD): <input type="text" name="proceeding_date" value="<%= proceeding.proceeding_date.toISOString().split('T')[0] %>" required></label><br>
    <label>Description: <textarea name="description" required><%= proceeding.description %></textarea></label><br>
    <label>Tentative Date (YYYY-MM-DD, optional): <input type="text" name="tentative_date" value="<%= proceeding.tentative_date ? proceeding.tentative_date.toISOString().split('T')[0] : '' %>"></label><br>
    <button type="submit">Update Proceeding</button>
  </form>
  <a href="/case/<%= proceeding.case_id %>">Back to Case</a>
</body>
</html>